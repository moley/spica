spica.model.projectInfos.each {
    println "Project " + it.name
}

println ""

spica.model.userInfos.each {
    println "User " + it.name
}

println ""

spica.model.topicInfos.each {
    println "Topic " + it.name
}